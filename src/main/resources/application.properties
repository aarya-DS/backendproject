#spring.application.name=backendp
#server.port=3333

#spring.datasource.url=jdbc:mysql://localhost:3306/backendp
#spring.datasource.username=root
#spring.datasource.password=root

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true


# Uncomment and set these if you prefer hardcoding values instead of using an env variable:
# spring.datasource.url=jdbc:postgresql://localhost:5432/backendp
# spring.datasource.username=youruser
# spring.datasource.password=yourpassword

# Active configuration uses environment variables. Provide defaults so app still starts locally:
# - DB_URL must begin with jdbc: (e.g. jdbc:postgresql://localhost:5432/backendp)
# - DB_USERNAME and DB_PASSWORD are optional and can be left blank.
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
server.port=${PORT:3333}
jdbc:postgresql://dpg-d6g16ihaae7s73ap7tu0-a/aaryak
